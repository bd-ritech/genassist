
ENV=prod

# API Keys - Replace with your actual keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GOOGLE_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
HUGGINGFACE_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

DB_HOST=localhost
DB_USER=postgres
DB_PASS=change-this-password-in-production
DB_NAME=core_db
DB_PORT=5432
# Aurora cluster reader endpoint for read-only queries. Leave empty to send all reads to DB_HOST.
DB_READ_HOST=
# Read-replica pool per tenant per app process, and the seconds a client keeps
# reading from the writer after its own write (0 disables).
DB_READ_POOL_SIZE=20
DB_READ_MAX_OVERFLOW=20
DB_READ_POOL_TIMEOUT=5
DB_READ_STATEMENT_TIMEOUT=600
DB_READ_PIN_AFTER_WRITE_SECONDS=5
DB_READ_FAILURE_COOLDOWN_SECONDS=30

# Seed users
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=change-me-admin-password
SEED_SUPERVISOR_USERNAME=supervisor1
SEED_SUPERVISOR_PASSWORD=change-me-supervisor-password
SEED_OPERATOR_USERNAME=operator1
SEED_OPERATOR_PASSWORD=change-me-operator-password
SEED_APIUSER_USERNAME=apiuser1
SEED_APIUSER_PASSWORD=change-me-apiuser-password
SEED_TRANSCRIBE_OPERATOR_USERNAME=transcribeoperator1
SEED_TRANSCRIBE_OPERATOR_PASSWORD=change-me-transcribe-operator-password

# Security - Generate strong random values for production
JWT_SECRET_KEY=replace-with-32-char-random-string-here
FERNET_KEY=replace-with-fernet-key-from-generate-key
USE_SSL=false

# Enable/disable WebSocket backend (connect, broadcast, rooms)
USE_WS=true
SSL_KEYFILE_PATH=./certs/key.pem
SSL_CERTFILE_PATH=./certs/cert.pem

DEBUG=true
FASTAPI_DEBUG=true
LOG_LEVEL=DEBUG
FASTAPI_RUN_PORT=8000
SQL_ALCHEMY_LOGGING_LEVEL=ERROR
RELOAD=true

# Enable/disable rate limiting
RATE_LIMIT_ENABLED=true
# "redis" or "memory"
RATE_LIMIT_STORAGE_BACKEND=redis
# Global rate limits
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000
# Auth endpoint rate limits (stricter)
RATE_LIMIT_AUTH_PER_MINUTE=5
RATE_LIMIT_AUTH_PER_HOUR=20
# Conversation endpoint rate limits
RATE_LIMIT_CONVERSATION_START_PER_MINUTE=10
RATE_LIMIT_CONVERSATION_START_PER_HOUR=100
# 30 conversation updates per minute per conversation (agent)
RATE_LIMIT_CONVERSATION_UPDATE_PER_MINUTE=30
# 500 conversation updates per hour per conversation (agent)
RATE_LIMIT_CONVERSATION_UPDATE_PER_HOUR=500

# AWS Credentials - Replace with your actual credentials
AWS_ACCOUNT_ID=xxxxxxxxxxxx
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIAxxxxxxxxxxxxxxxx
AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
AWS_RECORDINGS_BUCKET=your-recordings-bucket-name
#AWS_S3_TEST_BUCKET=your-test-bucket-name

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_SSL=false # set to true if using Redis SSL
#REDIS_USER=redis
#REDIS_PASSWORD=  # Optional; set for Redis AUTH (e.g. managed/cloud Redis)
#REDIS_OVERRIDE_URL= redis://localhost:6379  # Optional; set for Redis Cluster mode

# Redis connection pool settings
REDIS_FOR_CONVERSATION=true
REDIS_MAX_CONNECTIONS=10
REDIS_MAX_CONNECTIONS_FOR_ENDPOINT_CACHE=5
REDIS_SOCKET_TIMEOUT=10
REDIS_SOCKET_CONNECT_TIMEOUT=15 # seconds

# Celery Redis connection pool settings
CELERY_REDIS_MAX_CONNECTIONS=50

# Celery Beat task toggles (enable/disable periodic jobs)
CELERY_ENABLE_RUN_EXAMPLE_TASK=true
CELERY_ENABLE_CLEANUP_STALE_CONVERSATIONS_TASK=true
CELERY_ENABLE_IMPORT_S3_FILES_TASK=true
CELERY_ENABLE_TRANSCRIBE_S3_FILES_TASK=true
CELERY_ENABLE_ZENDESK_ANALYSIS_TASK=true
CELERY_ENABLE_IMPORT_ZENDESK_ARTICLES_TASK=true
CELERY_ENABLE_IMPORT_SHAREPOINT_FILES_TASK=true
CELERY_ENABLE_TRANSCRIBE_AUDIO_FILES_FROM_SMB_TASK=true
CELERY_ENABLE_SYNC_ACTIVE_FINE_TUNING_JOBS_TASK=true
CELERY_ENABLE_CHECK_SCHEDULED_PIPELINE_RUNS_TASK=true
CELERY_ENABLE_SUMMARIZE_FILES_FROM_AZURE_TASK=true
CELERY_ENABLE_AGGREGATE_AGENT_ANALYTICS_TASK=true

# Celery worker pool: "solo" avoids SIGSEGV with some ML libs.
# Use "prefork" only if workers do not import heavy ML libraries.
CELERY_WORKER_POOL=solo

PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python

# Opik Configuration
USE_OPIK=FALSE
OPIK_URL_OVERRIDE=https://www.comet.com/opik/api
OPIK_WORKSPACE=your-workspace-name
OPIK_API_KEY=your-comet-api-key
OPIK_PROJECT_NAME=your-project-name

# Zendesk Configuration
ZENDESK_SUBDOMAIN=your-subdomain
ZENDESK_EMAIL=your-email@example.com
ZENDESK_API_TOKEN=your-zendesk-api-token
ZENDESK_CUSTOM_FIELD_CONVERSATION_ID=0

# Help Center → company Azure DevOps Boards (platform; not per-user App Settings)
AZURE_DEVOPS_ORGANIZATION_URL=https://dev.azure.com/your-org
AZURE_DEVOPS_PROJECT=YourProject
AZURE_DEVOPS_PAT=your-personal-access-token
# Work item type per Help Center ticket type. Defaults match Azure Boards
# (bug -> Bug, feature -> Feature, task -> Task). Override only for non-standard
# process templates (e.g. Agile uses "User Story" instead of "Feature").
AZURE_DEVOPS_WORK_ITEM_TYPE=Bug
# AZURE_DEVOPS_FEATURE_WORK_ITEM_TYPE=Feature
# AZURE_DEVOPS_TASK_WORK_ITEM_TYPE=Task
# Azure Boards Area Path for Help Center (client-reported) work items, so they
# stay separate from work items you create manually.
# AZURE_DEVOPS_DEFAULT_AREA_PATH=YourProject
# AZURE_DEVOPS_WEBHOOK_SECRET=
# Public URL for links embedded in ADO work items (e.g. https://app.genassist.example.com)
# HELP_CENTER_PUBLIC_BASE_URL=

WHISPER_TRANSCRIBE_SERVICE=http://localhost:8001/transcribe

# Google/Microsoft API Keys
GOOGLE_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MICROSOFT_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
MICROSOFT_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx
MICROSOFT_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Vector Database Configuration
CHROMA_HOST=localhost # if in development with backend running inside IDE(non docker) otherwise remove this variable
CHROMA_PORT=8005 # if in development with backend running inside IDE(non docker) otherwise remove this variable
QDRANT_HOST=http://localhost
QDRANT_PORT=6333

# Multi-tenant Configuration
MULTI_TENANT_ENABLED=true
TENANT_HEADER_NAME=x-tenant-id
TENANT_SUBDOMAIN_ENABLED=False

CORS_ALLOWED_ORIGINS=*

# Optional regex an *unknown* (per-agent) Origin must match before AgentCORSMiddleware
# reflects it with Access-Control-Allow-Credentials. Matched with fullmatch against the
# whole Origin (scheme://host[:port]). Leave empty to reflect any non-static Origin
# (current behavior). Set it to close the CSRF vector if agent endpoints ever move to
# cookie-based auth. Example: https://([a-z0-9-]+\.)*example\.com
CORS_AGENT_ALLOWED_ORIGIN_REGEX=

# -----------------------------------------------------------------------------
# Microsoft Entra ID SSO (OIDC, confidential client)
# -----------------------------------------------------------------------------
# Master switch. When true, also set all required variables below and Redis.
SSO_MICROSOFT_ENABLED=false
# Entra directory (tenant) ID, or common / organizations / consumers if applicable
SSO_MICROSOFT_ENTRA_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# App registration (application) client ID and secret
SSO_MICROSOFT_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
SSO_MICROSOFT_CLIENT_SECRET=replace-with-client-secret
# Must exactly match a "Web" redirect URI in Entra (backend callback, not the SPA).
# Paths always include the /api prefix (see app registration → same host as your API).
# Example production: https://api.example.com/api/auth/sso/microsoft/callback
# Example local:   http://localhost:8000/api/auth/sso/microsoft/callback
SSO_MICROSOFT_REDIRECT_URI=https://api.example.com/api/auth/sso/microsoft/callback
# SPA origin used after login; redirect is {this}/login/sso-callback?sso_code=...
# Must be allowed by microsoft_sso_allowed_origins (this URL + CORS_ALLOWED_ORIGINS + optional list below)
SSO_MICROSOFT_POST_LOGIN_FRONTEND_URL=https://app.example.com
# Optional comma-separated extra allowed origins for that redirect (in addition to POST_LOGIN URL and CORS_ALLOWED_ORIGINS)
#SSO_MICROSOFT_POST_LOGIN_ORIGINS_ALLOWLIST=https://staging.example.com,https://app.example.com
# Create local user on first Entra login (requires defaults when true)
SSO_MICROSOFT_AUTO_PROVISION=false
# Comma-separated role UUIDs and single user_type UUID (required when AUTO_PROVISION=true)
#SSO_MICROSOFT_DEFAULT_USER_TYPE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
#SSO_MICROSOFT_DEFAULT_ROLE_IDS=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx,yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy

# reCAPTCHA Configuration
RECAPTCHA_ENABLED=False
RECAPTCHA_PROJECT_ID=your-project-id
RECAPTCHA_SITE_KEY=your-site-key
RECAPTCHA_MIN_SCORE=0.5 # 0.0 - 1.0
GCP_SVC_ACCOUNT=your-base64-encoded-credentials

# Azure Blob Storage
AZURE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-account;AccountKey=your-key;EndpointSuffix=core.windows.net
AZURE_ACCOUNT_NAME=your-account-name
AZURE_ACCOUNT_KEY=your-account-key
AZURE_CONTAINER_NAME=your-container-name

MSSQL_DRIVER=ODBC+Driver+18+for+SQL+Server

# Bedrock embedding
BEDROCK_MAX_RETRY_QUERY_EMBEDDING=3
BEDROCK_TIMEOUT_QUERY_EMBEDDING_SECONDS=8

# S3 Storage Bucket Name -> Files
AWS_BUCKET_NAME=your-s3-bucket-name
# File Manager
FILE_MANAGER_ENABLED=false
# File Manager Provider -> local, s3, azure, gcs, sharepoint
FILE_MANAGER_PROVIDER=local

# Train Data Source extraction ceilings. A workflow node may lower these with
# maxRows, maxBytes, or timeoutSeconds, but it cannot raise them.
ML_EXTRACT_MAX_ROWS=2000000
ML_EXTRACT_MAX_BYTES=2147483648 # 2 * 1024 * 1024 * 1024 bytes (2 GiB)
ML_EXTRACT_QUERY_TIMEOUT_SECONDS=600

# Direct browser -> S3 presigned PUT uploads (opt-in, requires FILE_MANAGER_PROVIDER=s3).
# When false (default), the existing /file-manager/upload and /file-manager/upload-session
# flows are used unchanged. When true, the frontend may obtain a presigned PUT URL and
# upload the file body directly to S3, then call /finalize.
FILES_DIRECT_S3_UPLOAD_ENABLED=false
# Lifetime of presigned PUT URLs returned to clients, in seconds.
FILES_DIRECT_S3_PRESIGN_EXPIRES_SECONDS=600

# Conversation cleanup stale minutes
CONVERSATION_CLEANUP_STALE_MINUTES=30

# Shared secret for internal WebSocket service authentication
WS_INTERNAL_SECRET=_service-to-service-secret_

# Conversation history max messages for chat input node
CONVERSATION_HISTORY_NODE_MAX_MESSAGES=50

# OpenTelemetry (FastAPI + workflow spans). Set USE_OTEL=true to enable SDK + FastAPIInstrumentor.
# Opik unified traces: OPIK_OTEL_EXPORT=true sends OTLP HTTP/protobuf to Comet (needs OPIK_* or OTEL_EXPORTER_OTLP_HEADERS with Authorization).
# Optional second sink (e.g. collector on gRPC 4317): OTEL_EXPORTER_OTLP_GRPC_ENDPOINT=localhost:4317
# Histogram genassist.workflow.node.duration_seconds exports via gRPC when OTEL_METRICS_VIA_GRPC=true (same endpoint).
# Celery: call init_opentelemetry() in the worker and propagate trace context if you need queue spans; Flower covers task monitoring meanwhile.
USE_OTEL=false
OPIK_OTEL_EXPORT=false
OPIK_OTEL_TRACES_ENDPOINT=
OTEL_EXPORTER_OTLP_GRPC_ENDPOINT=
OTEL_METRICS_VIA_GRPC=true
OTEL_SDK_DISABLED=false
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=your-key,projectName=your-project,Comet-Workspace=your-workspace
# OTEL_TRACES_SAMPLER_ARG=1.0

# Local fine tuning
LOCAL_FINE_TUNE_API_URL=http://192.168.1.10:9100/
# Shared HS256 secret used to mint short-lived JWTs for the upstream fine-tune service.
LOCAL_FINE_TUNE_JWT_SECRET=change-me
# Calling environment sent as `origin` in the minted JWT. Must match a key in
# the remote fine-tune service's REMOTE_FILE_SERVICE_URLS (e.g. "dev", "test", "prod").
LOCAL_FINE_TUNING_CALL_ORIGIN=dev
